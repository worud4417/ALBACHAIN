const message = {
    serverError : "server error",
    nullParam : "parameter is null",
    duplicatedParam : "duplicated parameter",
    idNotFounded : "id is not founded"
}

module.exports = message;